$kind: http-request
name: 'QUEUE-02 - Adicionar Livro 2 (Fundacao)'
method: POST
url: '{{baseUrl}}/api/book-clubs/{{clubId}}/queue'
order: 2000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{adminToken}}'
headers:
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "googleBooksId": "nnAqAAAAMAAJ"
    }
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      const json = pm.response.json();
      if (json.id) {
        pm.environment.set("queueEntry2Id", json.id);
        console.log("queueEntry2Id salvo:", json.id);
      }
      pm.test("Livro 2 adicionado à fila (200)", () => pm.response.to.have.status(200));
      pm.test("Resposta tem id", () => pm.expect(json.id).to.be.a("string"));
