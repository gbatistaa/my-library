$kind: http-request
name: 'QUEUE-02 - Adicionar Livro 2 (Fundacao)'
method: POST
url: '{{baseUrl}}/api/book-clubs/{{clubId}}/books'
order: 2000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{adminToken}}'
headers:
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "googleBooksId": "nnAqAAAAMAAJ",
      "deadline": "2026-07-31"
    }
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      const json = pm.response.json();
      if (json.id) {
        pm.environment.set("b2Id", json.id);
        console.log("b2Id salvo:", json.id);
      }
      pm.test("Livro 2 adicionado à fila (201)", () => pm.response.to.have.status(201));
      pm.test("Resposta tem id", () => pm.expect(json.id).to.be.a("string"));
