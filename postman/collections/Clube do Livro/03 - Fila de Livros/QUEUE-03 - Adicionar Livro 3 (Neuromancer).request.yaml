$kind: http-request
name: 'QUEUE-03 - Adicionar Livro 3 (Neuromancer)'
method: POST
url: '{{baseUrl}}/api/book-clubs/{{clubId}}/books'
order: 3000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{adminToken}}'
headers:
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "googleBooksId": "9GbwhgEACAAJ",
      "deadline": "2026-09-30"
    }
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      const json = pm.response.json();
      if (json.id) {
        pm.environment.set("b3Id", json.id);
        console.log("b3Id salvo:", json.id);
      }
      pm.test("Livro 3 adicionado à fila (201)", () => pm.response.to.have.status(201));
      pm.test("Resposta tem id", () => pm.expect(json.id).to.be.a("string"));
