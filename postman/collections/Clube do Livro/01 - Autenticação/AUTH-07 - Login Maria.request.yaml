$kind: http-request
name: 'AUTH-07 - Login Maria'
method: POST
url: '{{baseUrl}}/api/auth/login'
order: 7000
headers:
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "username": "maria_oli",
      "password": "Senha@123"
    }
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      const json = pm.response.json();
      const token = json.token || json.accessToken || json.access_token;
      if (token) {
        pm.environment.set("mariaToken", token);
        console.log("mariaToken salvo.");
      }
      pm.test("Login Maria (200)", () => pm.response.to.have.status(200));
      pm.test("Token recebido", () => pm.expect(token).to.be.a("string"));
