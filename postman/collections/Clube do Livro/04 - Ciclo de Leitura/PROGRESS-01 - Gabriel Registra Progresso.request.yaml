$kind: http-request
name: 'PROGRESS-01 - Gabriel Registra Progresso'
method: PATCH
url: '{{baseUrl}}/api/book-clubs/{{clubId}}/books/{{b1Id}}/progress/me'
order: 3000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{adminToken}}'
headers:
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "currentPage": 150
    }
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test("Progresso registrado (200)", () => pm.response.to.have.status(200));
