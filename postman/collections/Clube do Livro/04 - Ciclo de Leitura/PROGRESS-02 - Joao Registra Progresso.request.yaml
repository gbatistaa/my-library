$kind: http-request
name: 'PROGRESS-02 - Joao Registra Progresso'
method: POST
url: '{{baseUrl}}/api/reading-cycles/{{cycleId}}/progress'
order: 4000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{joaoToken}}'
headers:
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "currentPage": 200,
      "totalPages": 412,
      "notes": "A política das casas é fascinante."
    }
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test("Progresso registrado (200)", () => pm.response.to.have.status(200));
